{
  "name": "optimism-sandbox",
  "version": "0.0.1",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/**"
  ],
  "scripts": {
    "format": "prettier --ignore-path .gitignore --write \"**/*.{ts,js,tsx,jsx,json,md,sol}\""
  },
  "devDependencies": {
    "prettier": "^2.2.1",
    "prettier-plugin-solidity": "^1.0.0-beta.7"
  }
}
